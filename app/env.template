# Agent URLs from your Toolhouse deployment
# Run: npm run deploy:all (from project root), then npm run setup:env
# Replace the placeholders below with your actual URLs from the deploy output

VITE_ORCHESTRATOR_URL=
VITE_DEBATER_URL=
VITE_SYNTHESIZER_URL=
